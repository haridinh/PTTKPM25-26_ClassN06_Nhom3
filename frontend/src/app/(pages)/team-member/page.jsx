import TeamMembers from '../../../components/team-members/index';

const TeamMember = () => {
   return <TeamMembers />;
};

export default TeamMember;
export const metadata = {
   title: 'Nishu | Team Member',
};
